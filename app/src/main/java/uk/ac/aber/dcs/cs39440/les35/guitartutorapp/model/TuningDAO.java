package uk.ac.aber.dcs.cs39440.les35.guitartutorapp.model;

import android.arch.persistence.room.Dao;
import android.arch.persistence.room.Insert;
import android.arch.persistence.room.Query;

import java.util.List;


import uk.ac.aber.dcs.cs39440.les35.guitartutorapp.objects.Tuning;


public class TuningDAO {

   /* @Insert
    void insertTunings(Tuning[] tunings);

    /**
     * Query for getting a tuning from the database by its unique ID
     * @param id
     * @return
     */
    /* @Query("SELECT * FROM tunings WHERE id = :id")
    Tuning getTuningById(int id);

    /**
     * Query for getting all current tunings stored in the Room database tunings table
     */
    /*@Query("SELECT * FROM tunings")
    List<Tuning> getAllTunings();*/
}
